<?xml version="1.0" encoding="windows-1252"?>
<node id="31374" title="Roman" created="2000-09-07 10:38:58" updated="2005-07-27 11:49:12">
<type id="31663">
modulereview</type>
<author id="9346">
mirod</author>
<data>
<field name="doctext">
&lt;h3&gt;Description&lt;/h3&gt;
&lt;p&gt;&lt;a href="http://theoryx5.uwinnipeg.ca/CPAN/data/Roman/Roman.html"&gt;
    Roman&lt;/a&gt; is a module for conversion between Roman and Arabic numerals. &lt;/p&gt;
&lt;code&gt;
  use Roman;

  $arabic = arabic($roman) if isroman($roman);
  $roman = Roman($arabic);
  $roman = roman($arabic);&lt;/code&gt;
&lt;h3&gt;Why use Roman?&lt;/h3&gt;
  &lt;ul&gt;
    &lt;li&gt;to number list item numbers in roman format&lt;/li&gt;
    &lt;li&gt;to display dates in an MPAA approved format&lt;/li&gt;
  &lt;/ul&gt;
&lt;h3&gt;Why NOT use Roman?&lt;/h3&gt;
  &lt;ul&gt;
    &lt;li&gt;if you need Roman numbers above 4000 (or if you don't 
        need roman numbers!)&lt;/li&gt;
  &lt;/ul&gt;
&lt;p&gt;Note: the module does not have a Makefile.PL, so you will 
         have to copy it in your perl module path yourself
         that should be something like 
         &lt;tt&gt;/usr/lib/perl5/site_perl/5.6.0/&lt;/tt&gt;. Alternatively
         you can use &lt;tt&gt;ExtUtils::MakeMaker&lt;/tt&gt; to generate
         a Makefile.PL:&lt;/p&gt;
&lt;code&gt;perl -e 'use ExtUtils::MakeMaker; WriteMakefile(NAME =&gt; "Roman");' &lt;/code&gt;
&lt;h3&gt;Personal comments&lt;/h3&gt;
  &lt;p&gt;Roman is a little module that I found when I had to
     convert Roman numbered lists from XML to HTML. Instead
     of spending half an hour remembering how those guys
     counted then writing it myself it took me 5 minutes to
     install a generic solution. Cool!&lt;/p&gt;
  &lt;p&gt;I guess now with Unicode being available the module
     could be upgraded to handle more numbers.&lt;/p&gt;
&lt;p&gt;&lt;b&gt;Update&lt;/b&gt;: it might look like [Dominus] does not quite 
   like Roman: &lt;a href="http://perl.plover.com/#Roman"&gt;Roman.pm is a new contender for stupidest Perl module ever written&lt;/a&gt;.
   but he is actually talking about a different module, one he
   wrote himself and apparently never submitted to CPAN, which allows you to write things like $IV+$IV
   and get VIII as a result. &lt;/p&gt;</field>
<field name="itemdescription">
Counting in roman numbers</field>
<field name="usercomment">
Simple but convenient, limited to 4000</field>
<field name="identifier">
</field>
</data>
</node>
